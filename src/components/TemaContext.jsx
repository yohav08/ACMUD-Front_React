// En este archivo creamos y exportamos el contexto del tema,
// el cual será utilizado en toda la aplicación para compartir el estado del modo (claro/oscuro).

import { createContext } from "react";
export const TemaContext = createContext();